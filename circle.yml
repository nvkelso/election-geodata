machine:
  python:
    version: 2.7.11

dependencies:
  override:
    - sudo add-apt-repository -y ppa:ubuntugis/ppa
    - sudo apt-get update -y
    - sudo apt-get install -y python-mapnik python-gdal gdal-bin

test:
  override:
    - make
